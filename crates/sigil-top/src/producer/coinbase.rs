//! Producer-mode coinbase/reward logic — INERT STUB, Phase 1 (see `producer/mod.rs`).
//!
//! Phase 2 ports this from `sigil-node/src/coinbase.rs` (reward computation and the
//! master/commons dev-fee split, per that file's own docs) plus `sigil-node/src/
//! rate_governor.rs` (the adaptive demand-responsive block-rate governor it depends on).
