# bybit api key — SCRUBBED
# The credential was moved OUT of this repo (secrets must never live in a git tree).
# It is stored 600-perm off-repo at /home/storage/.bybit-env, and it tested INVALID (HTTP 401).
# Action needed: ROTATE the key at bybit.com → API, and put a VALID key+secret pair in /home/storage/.bybit-env.
