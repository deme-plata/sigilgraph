[package]
name = "delivery-probe"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Live-network measurement harness for the gossip delivery law D(p,r)=1-p^r — real flux-p2p request/response over kernel-degraded (netem) links"

[dependencies]
flux-p2p = { path = "../../../flux/crates/flux-p2p" }
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
